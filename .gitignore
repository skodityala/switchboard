node_modules/
dist/
*.tsbuildinfo
.DS_Store
*.log

# Credentials. Every adapter reads from the environment; nothing is ever
# committed. Adapter runbooks are in docs/adapters/.
.env
.env.*
!.env.example
*.pem
*.crt

# Local SQLite artifacts — fixtures are committed as .sql/.json seeds, not binaries
*.db
*.db-journal
*.sqlite
*.sqlite3
bench/dist/

# console/app.js is COMMITTED on purpose — see scripts/build-console.mjs.
# The demo must open from a bare clone with no build step and no toolchain.

# On-device model: fetched by `npm run fetch:model`, never committed.
# 22 MB of binary does not belong in a contest repo, and the DEFAULT
# deterministic path does not need it.
models/
