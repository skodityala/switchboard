# Public npm only. This repo must never depend on Apple-internal infrastructure:
# it is a public contest repo, and CI runners cannot reach an internal registry.
# The machine's user-level ~/.npmrc points at https://npm.apple.com — this
# repo-local file overrides it for every npm command run inside the repo.
registry=https://registry.npmjs.org
audit=false
fund=false
