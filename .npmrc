# Public npm only.
#
# This repo must never depend on a private or internal package registry: it is a
# public contest repo, and CI runners cannot reach one. This file overrides any
# user-level registry configuration for every npm command run inside the repo,
# so a clean clone resolves identically here and in CI.
registry=https://registry.npmjs.org
audit=false
fund=false
